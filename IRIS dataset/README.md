This Python script loads the Iris dataset, splits it into an 80-20 training-test set, and applies feature scaling using StandardScaler to normalize the numerical features for better model performance.

